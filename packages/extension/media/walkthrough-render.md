## Rendering

Press **Render** above the table.

Each page is rendered once and remembered, so rendering again after changing a
caption takes a moment rather than minutes. If a plot changed but the workbook
did not, use **Rebuild everything** — it ignores everything remembered.

The workbook lands in `.plotexcel/out/`, or wherever the `#output:` line at the
top of the layout says.
